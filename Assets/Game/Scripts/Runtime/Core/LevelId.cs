namespace Game.Core
{
    /// <summary>Los tres niveles narrativos, en el orden en que se desbloquean (RF-03).</summary>
    public enum LevelId
    {
        Fire = 1,
        Wheel = 2,
        River = 3
    }
}
